import React from "react";

const Searcher: React.FC = () => {
    return (
        <div>
            <h2>Search Page</h2>
            <p>This is the searcher view where users can search for something.</p>
        </div>
    );
};

export default Searcher;
